// SPDX-License-Identifier: MIT
// Copyright (c) 2026 TrendVidia, LLC.
//
// Invalid fixture: container_alias.
// MUST NOT compile (RFC-001 §5.1, §6.3): v1.2 forbids `repeated` and
// `map<,>` in typeRef — container-shaped alias targets are deferred to
// a future minor. Containers still hold typed elements
// (`repeated Email` at a field is fine); only the alias TARGET is
// restricted.

syntax = "proto3";

package fixtures.invalid.container;

import "protowire/schema/v1/annotations.proto";

type Tags = repeated string;
