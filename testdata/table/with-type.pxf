# §3.4.4 standalone. @table coexisting with @type. MUST be rejected — the
# @table header is itself the document's type declaration.

@type test.v1.AllTypes

@table test.v1.AllTypes (string_field, int32_field)

("row-1", 1)
