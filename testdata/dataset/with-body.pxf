# §3.4.4 standalone. @dataset coexisting with a top-level field_entry.
# MUST be rejected — a @dataset document has no body field entries.

@dataset test.v1.AllTypes (string_field, int32_field)

("row-1", 1)

# Forbidden — field_entry at document root alongside @dataset.
string_field = "stray"
